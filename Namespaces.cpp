#include <iostream>
 
    using namespace std;
    
       int main () {
    //namespaces = provides a solution for preventing name  confilicts
	//             in large projects 
	//             for each entity needs a unique name, a  namespace 
	//             allows for identically named entities as long as the 
	//             namespaces are different
	
	// because the variable x has already been declared, it can't be 
	// redaclared that's it will run into an error that variable x has already
	// been declared
     	//int x = 0;
		//int x = 1;
	   	
       	
       	
       	
       	
       	return 0;
	   }